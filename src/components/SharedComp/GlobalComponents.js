import Vue from "vue";
import veButton from "../SharedComp/VEButton.vue";
import VEModel from "../SharedComp/VEModel.vue";

//Global Button Component to use in overall Project
Vue.component(veButton.name, veButton);
Vue.component(VEModel.name, VEModel);
